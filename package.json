{
  "name": "settlekit",
  "version": "0.0.0",
  "private": true,
  "description": "SettleKit Commerce OS — sell software, private repos, SaaS, APIs, and AI tools in USDC with automatic access delivery.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
